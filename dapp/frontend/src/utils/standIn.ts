// What a claim button leaves behind when it cannot be offered. Owned here rather than passed in,
// because all four call sites were repeating the same run and only differ by how they align it.
export const standInClass = 'inline-flex items-center gap-1.5 font-mono text-xs text-fg-muted'
