// localStorage key `<ThemeProvider>` writes the mode to unless given another.
export const DEFAULT_STORAGE_KEY = 'cnc-theme'

// The OS-preference query `system` mode resolves against.
export const DARK_QUERY = '(prefers-color-scheme: dark)'
